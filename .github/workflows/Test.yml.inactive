name: Swift Lint
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - name: Swift Build
        run: swift build -v
      - name: Swift Test
        run: swift test -v
